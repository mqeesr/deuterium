#include <iostream>
#include <cmath>
using namespace std;

int main(){
    double res = floor(1.2); // output = 1
    cout << res;
    double x, y;
    double result = pow(x, y); // x to the power of y
    
    return 0;
}