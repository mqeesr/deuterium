#include <iostream>
#include <cmath>
using namespace std;

int main(){
    double x = 3;
    double y = 4;
    double z;

    //z = min(x, y);
    //z = pow(2, 3);
    //z = sqrt(9);
    //z = abs(-3); positive number
    //z = round(x); 
    //z = ceil(x); round up
    //z = floor(x); round down
    
    cout << z;
    return 0;
}